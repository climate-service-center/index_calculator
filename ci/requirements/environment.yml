name: xcalc-tests
channels:
    - conda-forge
    - nodefaults
dependencies:
    - dask
    - xarray
    - numpy
    - xclim>=0.56
    - netCDF4
    - pytest
    - pytest-cov
    - pytest-env
    - pooch
    - pint

    - pip:
      - pyhomogenize>=0.5.1
