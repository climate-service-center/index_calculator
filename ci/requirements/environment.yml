name: xcalc-tests
channels:
    - conda-forge
    - nodefaults
dependencies:
    - dask
    - xarray
    - xclim<=0.48
    - netCDF4
    - pytest
    - pytest-cov
    - pytest-env
    - pooch

    - pip:
      - pyhomogenize>=0.5.1
